Output only valid MathML {math_ml_version} for the tagged Formula: a single well-formed root declaring the MathML namespace, matching the expression in the image, every tag closed, no markdown, prose, or text outside the MathML.
