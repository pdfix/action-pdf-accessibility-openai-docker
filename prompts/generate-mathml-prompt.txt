Return only the full and valid XML representation of the mathematical expression in MathML {math_ml_version} format, with no markdown, no comments, and no explanation.

The output must:
- Begin with the opening <math> tag with the correct namespace.
- Do not stop generating until every opened XML tag is properly closed, ending with </math>.
- If the XML would be truncated, continue until the <math> element is fully closed.
- Contain valid and well-formed XML.
- Not include any incomplete or truncated tags or elements.
- Be a complete representation of the expression shown in the image.

Do not include any text before or after the XML.
Only output the MathML content.