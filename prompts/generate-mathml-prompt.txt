Output only valid MathML {math_ml_version}: a single well-formed root element declaring the MathML namespace, matching the formula in the image, every tag closed, no markdown, prose, or text outside the MathML.
